const path = "/users/download/index.css"

const isCss = path => {
  const requiredExt = ".css";
  const pathExt = path.slice(-4);

  return pathExt === requiredExt
}

console.log(isCss(path));